-- permitir parametros en los scripts
-- onda, -set N 15 M 20
-- order n N : S (default 10)

order n N : node.
order t M : tile.
order d 10 : depth.

let node.level : level.
let node.

-- definicion de <=

 -> n <= m.


-- la bounding box de los nodos raiz contiene a las bounding boxes de los nodos parte

part (a, b) -> b.x <= a.x.
part (a, b) -> b.0x >= a.0x.
part (a, b) -> b.y <= a.y.
part (a, b) -> b.0y <= a.0y.

-- la relacion 'parte' es antisimetrica


-- la relacion parte permite solapamiento?


-- accesibilidad


-- movimiento


-- cuevas


